syntax = "proto3";

package ping;

option go_package = "github.com/ns9ryan/p9_core/rpc/pb/core/ping";

// Ping 请求
message PingRequest {}

// Ping 响应
message PingResponse {
  // 响应消息
  string msg = 1;
}